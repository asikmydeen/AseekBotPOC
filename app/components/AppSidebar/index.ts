// This file provides backward compatibility for existing imports
// It re-exports the new AppSidebar component from its new location

import AppSidebar from '../sidebar/AppSidebar';

export default AppSidebar;
